'use babel';

import LinterStyledComponentsView from '../lib/linter-styled-components-view';

describe('LinterStyledComponentsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
